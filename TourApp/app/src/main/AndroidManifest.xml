<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.uniba.gruppo5.tourapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:required="true" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tourapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tourapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MostraImmagineActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".CategorieAttrazioniActivity" />
        <activity
            android:name=".DiarioActivity"
            android:label="@string/title_activity_diario"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity android:name=".SetupActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".RegistrazioneActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".DettaglioDiarioActivity" />
        <activity android:name=".AttrazioniActivity" />
        <activity android:name=".DettaglioAttrazioneActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cms.CmsHomeActivity" />
        <activity android:name=".cms.CmsUtentiActivity" />
        <activity android:name=".cms.CmsDatiGeneraliActivity" />
        <activity android:name=".cms.CmsUtenteDetailActivity" />
        <activity android:name=".cms.CmsCategoriaCouponDetailActivity" />
        <activity android:name=".cms.CmsCategorieCouponActivity" />
        <activity android:name=".cms.CmsCouponActivity" />
        <activity android:name=".cms.CmsCouponDetailActivity" />
        <activity android:name=".cms.CmsCategoriaAttrazioneDetailActivity" />
        <activity android:name=".cms.CmsCategorieAttrazioneActivity" />
        <activity android:name=".cms.CmsTagAttrazioneDetailActivity" />
        <activity android:name=".cms.CmsTagAttrazioneActivity" />
        <activity android:name=".cms.CmsAttrazioneDetailActivity" />
        <activity android:name=".cms.CmsAttrazioniActivity" />
        <activity android:name=".CouponActivity" />
        <activity android:name=".DettaglioCouponActivity" />
        <activity android:name=".CouponPreferitiActivity" />
        <activity android:name=".UtilizzoCouponActivity" />
        <activity android:name=".ElencoDispositiviActivity" />
        <activity
            android:name=".LocationActivity"
            android:label="@string/title_activity_esplora" />

    </application>


</manifest>